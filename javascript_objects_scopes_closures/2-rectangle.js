#!/usr/bin/node
//Class rectangle that is initialized by the following constructors

class Rectangle{
    constructor(w, h){
        if (w>0 && h>0){
            this.width = w;
            this.height = h;

        };

        };
};
module.exports = Rectangle;